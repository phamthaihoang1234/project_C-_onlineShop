@model IEnumerable<Ictshop.Models.Brand>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<<<<<<< HEAD
<h2>Brand Management</h2>
<!--<form action="@Url.Action("Index", "Brands")" id="search-form" >
    <div id="search-autocomplete" class="form-outline">
        <input type="search" id="txt-search" class="form-control" name="name"
               value="@ViewBag.searchName"  
               placeholder="Search By Name ..."/>
        <button class="btn btn-success"> Search </button>-->
@*        <i class="fas fa-search"></i>*@
    <!--</div>
</form>-->
=======
<h2>Quản lý hãng điện thoại</h2>

>>>>>>> a2fba8a0e9701361a9b5c8bb1c98b922dc39d975
<p>
    @Html.ActionLink("Thêm mới hãng", "Create")
</p>
<table class="table">
    <tr>

        <th>
            Mã hãng
        </th>
        <th> Tên hãng
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.BrandID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.BrandName)
    </td>
    <td>
        @Html.ActionLink("Sửa", "Edit", new { id = item.BrandID }) |
        @Html.ActionLink("Chi tiết", "Details", new { id = item.BrandID }) |
        @Html.ActionLink("Xoá", "Delete", new { id = item.BrandID })
    </td>
</tr>
}

</table>
