@model IEnumerable<Ictshop.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2> Category mangament </h2>
<!--<form action="@Url.Action("Index", "Categorys")" id="search-form">
    <div id="search-autocomplete" class="form-outline">
        <input type="search" id="txt-search" class="form-control" name="name"
               value="@ViewBag.searchName" oninput="LoadData()"
               placeholder="Search By Name ..." />-->
        @*        <i class="fas fa-search"></i>*@
    <!--</div>
</form>-->
<p>
    @Html.ActionLink("Add New Category", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Category ID 
        </th>
        <th>
            Category Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.CateID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CateName)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.CateID }) |
        @Html.ActionLink("Details", "Details", new { id = item.CateID }) |
        @Html.ActionLink("Delete ", "Delete", new { id = item.CateID })
    </td>
</tr>
}

</table>
<script>
    function LoadData() {
        @*window.location.href = "Admin/Brands/Index";*@
        $("#search-form").submit();
    }
</script>